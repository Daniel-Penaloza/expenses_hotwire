<%= turbo_frame_tag item do %>
  <ul class="list-group">
    <li class="list-group-item d-flex justify-content-between align-items-center bg-light m-2">
      <span class="fw-bold p-2 text-capitalize"><%= item.name %></span>
      <span><%= item.amount %></span>
    </li>
  </ul>
<% end %>